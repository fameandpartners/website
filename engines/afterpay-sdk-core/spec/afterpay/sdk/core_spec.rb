require 'spec_helper'

describe Afterpay::Sdk::Core do
  it 'has a version number' do
    expect(Afterpay::Sdk::Core::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
