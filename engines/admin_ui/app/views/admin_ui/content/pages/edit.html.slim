.pages.wrapper.wrapper-content.animated.fadeInRight
  = form_for page, :url => content_page_path(page), :html => {:class => 'form-horizontal'} do |f|
    = render :partial => 'form', :locals => { :f => f }
