$('.accordion-title[data-step="registration"]').removeClass('active');
$('.accordion-content[data-step="registration"]').slideUp();
$('.accordion-title[data-step="delivery"]').addClass('active');
$('.accordion-content[data-step="delivery"]').slideDown();
