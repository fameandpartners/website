$error = $('<span class="error" style="color: red; display: block;"/>').text('Check code and try again.');
$('.promo-block form .error').remove();
$('.promo-block form input[name*="coupon_code"]').after($error);
