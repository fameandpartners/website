= render :partial => 'spree/admin/shared/product_sub_menu'

= render :partial => 'spree/shared/error_messages', :locals => { :target => @making_option }

= form_for [:admin, @making_option], :html => { :multipart => false } do |f|

  fieldset data-hook="new_product_making_option"
    legend align="center"
      = t(:new_product_making_option)

    = render :partial => 'form', :locals => { :f => f }

    div.form-buttons.filter-actions.actions data-hook="buttons"
      = button t('actions.create'), 'icon-ok'
      span.or = t(:or)
      = link_to_with_icon 'icon-remove', t('actions.cancel'), admin_product_making_options_path, :class => 'button'
