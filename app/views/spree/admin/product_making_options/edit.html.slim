- content_for :page_title do
  = t(:edit_product_making_option)

- content_for :page_actions do
  .toolbar data-hook="toolbar"
    ul.toolbar.inline-menu
      li#product_making_options_link
        = link_to 'Back to Making Options', admin_product_making_options_path(@product), class: 'icon-arrow-left button'

= render 'spree/admin/shared/product_sub_menu'

= render 'spree/admin/shared/product_tabs', current: 'Making Options'

= render partial: 'spree/shared/error_messages', locals: { target: @product_making_option }

= form_for @product_making_option, url: admin_product_making_option_path(@product, @product_making_option) do |f|
  fieldset.no-border-top
    = render :partial => 'form', :locals => { :f => f }

    .clear

    .form-buttons.filter-actions.actions
      = button t(:update), 'icon-refresh'
      span.or =<> t(:or)
      = button_link_to t(:cancel), admin_product_making_options_path(@product), icon: 'icon-remove'
