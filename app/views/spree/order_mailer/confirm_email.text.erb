Good news, your order <%= @order.number %> has been placed.

Thank you for shopping at Fame and Partners. We are so excited to send your order.
You will receive another email with your tracking number once your order has been shipped.

============================================================
You ordered [<%= @order.number %>]:
============================================================
<% @order.line_items.each do |item| %>
<%= item.variant.sku %> <%= raw(item.variant.product.name) %> <%= item.making_options_text %> <%= raw(item.options_text) -%> (<%=item.quantity%>) @ <%= item.variant.display_amount %> = <%= item.display_amount %> Estimated delivery, <%= item.delivery_period %>
<% end %>
============================================================
<%= t('order_mailer.confirm_email.subtotal', :subtotal => @order.display_item_total) %>

<% @order.adjustments.eligible.each do |adjustment| %>
  <%= raw(adjustment.label) %> <%= adjustment.display_amount %>
<% end %>

<%= t('order_mailer.confirm_email.total', :total => @order.display_total) %>

<%= t('order_mailer.confirm_email.thanks') %>

To track your order or manage your personal details, log in to your account here: <%= login_url %>

<% if @order.user && @order.user.automagically_registered?  %>
We have automatically created an account for your convenience where you can track your order and manage your personal details.

Your username is: <%= @order.email %>
Your temporary password is: <%= @order.email %>

Please change this password once you have logged in.
<% end %>

When your order arrives:
Try it on! Here are some Handy tips:
Keep all packaging in case you need to make a return, exchange or refund.
Please make sure the dress is unworn and all tags are still attached.
Please contact team@fameandpartners.com for a RMA number within 30 days of receiving your order.
