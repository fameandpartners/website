Welcome <%= @user.full_name %>

You now have access to the most beautiful formal dresses available anywhere. Inspired by the hottest trends right now. Take a look to see the difference yourself.

Thanks for signing up with Fame and Partners.

your email:
  <%= @user.email %>
your pass:
  <%= @user.password %>

competition url: '<%= main_app.user_competition_boutique_path(user_id: @user.slug, competition_id: Competition.current) %>

Our Collection
<%= main_app.collection_url %>

About
<%= main_app.about_us_url %>
