= item.attributes
= cropped_product_hoverable_image_tag(item.product, css_class: '')
= link_to 'Remove', moodboard_item_path(item.moodboard_id, item.id),  method: :delete, data: { confirm: 'Are you sure?' }
