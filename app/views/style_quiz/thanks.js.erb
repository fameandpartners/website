<% if false %>
  $('.quiz-box-inner').html('<%=j render(:template => 'style_quiz/thanks.html.slim') %>');
<% end %>

if (window.parent && window.parent.onQuizCompleted) {
  window.parent.onQuizCompleted({});
} else {
  window.location = "<%= after_quiz_redirect_url %>";
};
