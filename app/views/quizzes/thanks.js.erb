$('.quiz-box-inner').html('<%=j render(:template => 'quizzes/thanks.html.slim') %>');

if (!window.Quiz.redirectUrl) {
  window.Quiz.redirectUrl = "<%= my_boutique_url %>";
}
setTimeout(function(){
  window.location = window.Quiz.redirectUrl;
}, 3000);

track.quizFinished('<%=j analytics_label(:user) %>');
