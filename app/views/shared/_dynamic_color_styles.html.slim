ruby:
  colours = %w(black-and-navy taffeta-monochrome black-and-ivory black-on-cream black-and-white cherry plum black-with-black-bottom dark-blue black-on-pale-pink)
  colours = Spree::OptionValue.colors.where(name: colours)
  black = Spree::OptionValue.colors.find_by_name('black')
css:
  
  #{([black] + black.similars + colours).map{ |color| "body .color.#{color.name}" }.join(",\n")} {
    color: #fff;
  }

  #{dynamic_colors.map{ |color| "body .color.#{color.name} { background-color: #{color.value}; }"}.join("\n")}
