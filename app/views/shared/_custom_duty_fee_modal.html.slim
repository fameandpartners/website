#modal-custom-duty-fee-template.hidden

coffee:
  $ ->
    if $("#modal-custom-duty-fee-template").length != 0
      if $.cookie('custom-duty-fee-modal-displayed') != 'true'
        addToCartModal = new window.page.EmailCaptureModal({
          promocode: "",
          content: "",
          container: "#modal-custom-duty-fee-template",
          heading: "<span style=\"font-size:0.6em;\">Additional Custom Duty Fees May Apply</span>",
          message: "<div style=\"font-size:0.7em; padding-left:1rem; padding-right:1rem;\">" +
                  "<div><div>UK</div><div>Canada</div><div>New Zealand</div></div>" +
                  "<div style=\"margin-top:10px;\"><a class=\"btn btn-black\" onclick=\"vex.closeAll();\">OK</a/></div>" +
                  "</div>",
          className: "classic-modal welcome-modal",
          timeout: 0,
          timer: false,
          force: false
        });
        $.cookie('custom-duty-fee-modal-displayed', 'true')
