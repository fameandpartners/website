#modal-cny-template.hidden

coffee:
  $ ->
    if $("#modal-cny-template").length != 0
      if $.cookie('cny-modal-displayed') != 'true'
        addToCartModal = new window.page.EmailCaptureModal({
          promocode: "",
          content: "",
          container: "#modal-cny-template",
          heading: "<span style=\"font-size:0.7em;\">Made to Order</span>",
          message: "<div style=\"font-size:0.7em; padding-left:1rem; padding-right:1rem;\">" +
                  "<div>Every Fame and Partners dress is handmade to order by one of our seamstresses. </div>" +
                  "<div>PPlease allow 10-14 business days for creation and shipping of your dress.<div>" +
                  "<div style=\"margin-top:10px;\"><a class=\"btn btn-black\" onclick=\"vex.closeAll();\">OK</a/></div>" +
                  "</div>",
          className: "classic-modal welcome-modal",
          timeout: 0,
          timer: false,
          force: false
        });
        $.cookie('cny-modal-displayed', 'true')
