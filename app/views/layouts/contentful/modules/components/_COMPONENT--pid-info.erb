<% if pid_name && pid_price && pid_url %>
  <div class="Product">
    <a href="<%= pid_url %>">
      <p class="Product__name"><%= pid_name %></p>
      <p class="Product__price"><%= pid_price %></p>
    </a>
  </div>
<% end %>
