html = '<%=j  render(template: 'users/styleprofiles/show.html') %>';

$('body > .page-title').remove();
$('body > .page-content').replaceWith(html);

window.setCurrentPath('<%= styleprofile_path %>')

$('body > .page-content .show-style-quiz').on("click", function(event){
  event.preventDefault();
  event.stopPropagation();
  Quiz.show();
})

<% if @title.present? %>
  document.title = '<%=  @title %>';
<% end %>
