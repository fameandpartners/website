javascript:
  $(document).ready(function() {
    // enable link
    (function() {
      $('#next-button').prop('disabled', false);
      $('#next-button').on('click', function(e) {
        e.preventDefault();
        if ($('.bridesmaid-pallete .pallete-color-group.selected').length == 0){
          console.log("please, select at least one color");
          return false;
        } else {
          $('form#palette_form').submit();
          $(e.currentTarget).prop('disabled', false);
        };
      });
    })(document);

    // enable paletter
    (function() {
      $('.bridesmaid-pallete .pallete-color-group').on('click', function(e) {
        $item = $(e.currentTarget)
        $item.toggleClass('selected');
        result = _.map($('.bridesmaid-pallete .pallete-color-group.selected'), function(item) {
          return $(item).data('code');
        });
        $('#info_colours').val(result);
      });
    })(document);
  })

.bridesmaid-info-header
  = image_tag "_bridesmaid/pic3.jpg"
  .text-box
    .big Colour Palette
    .small Select the colours that you would prefer your dresses in.

= form_for @bridesmaid_event, as: :info, url: bridesmaid_party_colour_path, method: :put, html: { id: 'palette_form' } do |f|
  = f.hidden_field 'colours', { value: @palette.selected.join(','), id: 'info_colours' }

  .bridesmaid-pallete
    - @palette.color_groups.each do |group|
      - selection_status = @palette.selected.include?(group.name) ? 'selected' : ''
      .pallete-color-group data-code=(group.name) class=(selection_status) class=(group.presentation.downcase)
        = group.presentation

  = link_to '#', class: 'block-with-triangle', id: 'next-button' do
    span.text NEXT STEP
    span.triangle-bottom style="border-top-color: #e6a49d"
