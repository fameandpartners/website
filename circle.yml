machine:
  ruby:
    version: 2.1.5
  services:
    - elasticsearch

test:
  override:
    - bundle exec rspec spec engines --color spec --format progress # Default: `bundle exec rspec --color spec --format progress`
